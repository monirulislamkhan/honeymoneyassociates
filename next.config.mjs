/** @type {import('next').NextConfig} */
const nextConfig = {
  trailingSlash: true,
  poweredByHeader: false,
  optimizeFonts: true,
  reactStrictMode: true,
  swcMinify: true,
};

export default nextConfig;
